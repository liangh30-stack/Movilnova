{
  "name": "movilnova-functions",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google/genai": "^1.42.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "resend": "^6.9.3",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
